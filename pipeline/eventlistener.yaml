apiVersion: triggers.tekton.dev/v1alpha1
kind: EventListener
metadata:
  name: ansible-listener
  namespace: ansible-pipeline
spec:
  serviceAccountName: pipeline
  triggers:
    - name: trigger
      bindings:
        - ref: gitea-triggerbinding
      template:
        ref: feature-environment
    # - name: trigger
    #   bindings:
    #     - ref: gitea-triggerbinding
    #   template:
    #     ref: gitea-triggertemplate
